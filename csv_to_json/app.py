# I will try to use a minimal set of libraries.

# i never use good structure in my code. let's set if this works


def run(csv_in: str, json_out: str):
    print(f"csv: {csv_in}")
    print(f"json: {json_out}")
